#include<bits/stdc++.h>
#include<algorithm>
// [ l , r)
// sorts in non decreasing order

using namespace std;
int main()
{
    std::vector<int> data = {40, 10, 30, 20};
    cout << "Type of data.begin(): " << typeid(decltype(data.begin())).name() << "\n";
    
}
